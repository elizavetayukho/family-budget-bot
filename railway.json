{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm run build && cd api && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd api && npx prisma migrate deploy && cd .. && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
