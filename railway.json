{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install && cd api && npx prisma generate && cd ../bot && npx prisma generate && cd .. && npm run build"
  },
  "deploy": {
    "startCommand": "cd api && npx prisma migrate deploy && npx ts-node prisma/seed.ts && cd .. && node api/dist/index.js & node bot/dist/index.js & wait",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
